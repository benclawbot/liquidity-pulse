services:
  - type: web
    name: liquidity-pulse-api
    runtime: python
    buildCommand: pip install -r _backend/requirements.txt
    startCommand: uvicorn _backend.main:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"